import { StyleSheet } from "react-native";

export default StyleSheet.create({
  title: {
    margin: 20,
    color: "#FFFFFF",
    fontStyle: "italic",
    fontWeight: "900",
    fontSize: 60
  }
});
